{
    "name": "snorlax",
    "private": true,
    "workspaces": [
        "snorlax/*"
    ],
    "scripts": {
        "dev": "bun run --parallel --filter \"*\" dev",
        "build": "bun run --filter \"*\" build"
    }
}